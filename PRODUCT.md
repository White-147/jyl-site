# Product

## Register

brand

> 主界面是展示性单页（版式即产品），但信息组织按"招聘方可用"的产品纪律来做。以 `brand` 为默认 register；涉及预览页等工具性界面时按 `product` 处理。

## Users

**主要用户：中国大陆的招聘方。**
- 技术面试官 / 用人部门负责人：在 27 寸显示器前快速判断技术广度与项目真实度，关心项目细节、技术栈、可验证性。
- HR / 猎头：常常不是技术背景，在 5 到 10 秒内判断"这个人跟我手上的岗位对不对得上"，关心岗位方向、年限、地点、联系方式。
- 使用场景：白天办公室（浅色为主），移动端多为通勤路上或面试间隙（约 60% 流量来自手机）。
- 要完成的事：确认候选人方向匹配 → 下载简历 → 在线体验项目 → 联系约面。

**次要用户：同行与技术社区。** 通过 GitHub 或分享链接进入，关心实现方式与代码质量。

## Product Purpose

蒋宇龙的个人求职作品集单页，面向"信息技术综合岗位"，围绕数据工程、业务系统交付、Windows 原生桌面端工程化三条主线展示可验证项目。

成功标准：

1. 招聘方在首屏就能确认"方向匹配"，不需要滚动到第三屏。
2. 每个项目都能被独立验证：截图可放大、可在线体验、代码在 GitHub。
3. 简历 PDF 一键可得，且流转范围可控（带归属水印）。
4. 站内内容与投递简历口径一致，公司名、项目名、时间线不冲突。

## Brand Personality

**克制、工程感、可验证。**

- 语气：陈述事实，不自我营销。用数字与结果说话，不用形容词堆砌。
- 情绪目标：让招聘方感到"这个人做事有条理、信息密度高、不需要我费劲找东西"。
- 视觉气质："石墨 · 琥珀"。纯中性石墨打底（黑 / 白 / 灰三档），唯一强调色是暖琥珀；浅色与深色同族，只有明度方向相反。展示层用得意黑，名字用柳建毛草（草书是本人授意的品牌资产，即使首屏不出现，简历与站内其它位置也有同名文本，不构成信息缺失）。
- 明确不做：不炫技、不做情绪化的大色块、不做需长时间等待的入场动画。

## Anti-references

- **AI 生成感**：紫色渐变 + 星芒图标的 hero、"浮动仪表盘叠在渐变上"、三列等大特性卡、统一 bento 盒阵（"AI loves boxes"）。依据：公开的 AI 设计特征数据集里，默认组件库样式与紫色渐变是最高频的"AI 味"信号。
- **滚动劫持**：任何覆盖或改写滚动速度的做法，评论区一致反感。
- **模板式毛玻璃当默认**：`rgba(255,255,255,0.3)` + 一个固定 `blur()` 值糊在所有卡片上。**禁的是这个套路，不是玻璃本身**（2026-09 第四轮澄清）。允许的玻璃是**透明 + 边缘/整面折射**（`backdrop-filter: url(#lg-refract*)` + `feDisplacementMap`），且只用在两类地方：需要透视滚动内容的固定/浮层元素，以及每个区块的主容器。区块内的小卡不加模糊，走色阶。
- **过时装饰性交互**：自定义光标、十字光标跟随、磁性按钮、回弹（bounce）缓动、逐元素入场动画。
- **不安全的展示**：不公开手机号、住址、身份证等敏感字段；不提供无归属标记的简历原文件。
- **不可打印的幻觉**：本站不提供打印版式，打印一律拦截并引导到 PDF 简历。

## Design Principles

1. **先匹配，再惊艳。** 首屏解决"方向对不对得上"，视觉表达服从于此。招聘方的时间预算以秒计。
2. **信息密度优先于留白表演。** 留白用来分段，不用来制造"高级感"。任何一段的空白如果不能帮助理解，就是浪费。
3. **可验证胜过可描述。** 每个主张都要有出口：截图、在线体验、仓库、简历。文案只是索引。
4. **一处定义，处处一致。** 颜色、圆角、动效时长、网格纹理只在 token 层定义一次；组件不写魔法值。新页面新组件必须复用同一套语言。
5. **性能是设计的一部分。** 合成开销、字体体积、动画数量都是设计决策。中低端安卓与微信内置浏览器是硬约束，不是边界情况。

## Accessibility & Inclusion

- **基线：WCAG 2.2 AA。** 正文对比度 ≥ 4.5:1，大字 ≥ 3:1，图形边界 ≥ 3:1。EAA 自 2025 年 6 月起强制，作为出海与合规的前置条件一并满足。
- 全站尊重 `prefers-reduced-motion`，降级为交叉淡入而非删除功能。
- 键盘可达：焦点环始终可见，模态框有焦点陷阱与焦点归还，不拦截浏览器缩放与 200% 文字放大。
- 触控目标不小于 24×24 CSS px（移动端导航按 44px 设计）。
- 中文页面 `lang="zh-CN"`；内嵌英文技术名词标 `lang="en"`，避免读屏按中文读法念。
- **允许文本选中与复制**，仅在图片上拦截长按保存。全站禁止选择会阻断招聘方复制邮箱这一核心任务。
- 打印一律拦截并给出明确指引，避免输出半成品的排版。
