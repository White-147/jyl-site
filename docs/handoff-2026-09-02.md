# 个人作品集网站与 BookRecommendation 交接说明（2026-09-02）

> 本文档用于新会话快速接手：站点全部已有改动、未完成事项、BookRecommendation 优化方案与各预览的构建/同步流程。

## 一、概况

- 作品集站点：`D:\code\jyl-site`（React 19 + Vite + Tailwind 4，内容源 SQLite `database/portfolio.db`，构建自动 `db:export`，GitHub Actions 推 main 即部署到 https://white-147.github.io/jyl-site/）。
- 数据流：改 `src/data/*.json` → `npm run db:seed`（JSON→SQLite）→ `npm run build`（db:export + 类型检查 + 打包）。**注意 db 为上源**，改数据必须在 json 与库里同步（seed 前先改 json）。
- 相关源仓库（各自可独立提交）：`MiLuStudio`、`XiaoLouAI`、`MiLuAssistantWeb`（console 前端在 `console/`，Docker 构建）、`SyLabAI`（apps/web）。

## 二、已完成并部署（近期）

### 预览体系
1. **MiLuStudio 演示模式补全**：`apps/web/src/shared/mock/studioDemoMock.ts`（3 个示例项目全量数据：故事文本/图谱/分集/改编/脚本/角色设定），各 `shared/api/*` 接入 `VITE_EMBEDDED_DEMO` 分支；确认/重做可推进流程；修复"二次进入卡在正在读取"（保存 jobId 分支未缓存运行时 → 任务错配 projectId）、"首次进入默认打开第一个示例项目 + 宽屏≥768 初始展开侧栏"（手机端侧栏后续单独优化）。
2. **MiLuAssistantWeb console**：修复主题切换图标丢失（basename 改模块级一次性解析 `new URL('.', location.href).pathname`；新增 `src/utils/publicAssets.ts` 资产绝对路径）；Google Fonts 异步（preload+onload，不阻塞首屏）；100dvh 高度链（html/body/.ant-layout/.mainLayout/.sider/登录页）＋ `.page-content`/`.page-container` 加 `min-height:0`；Chat 模型引导提示改绝对定位悬浮（**根因：演示提示条与引导提示占流把输入区挤出**，曾出现"黑边/输入区下压"回归，已修复：polish 顶部条改浮层 + 引导条悬浮化）。
3. **XiaoLouAI**：根容器 `h-screen → h-screen supports-[height:100dvh]:h-[100dvh]`（iPad/移动端不再裁切）。
4. **SyLabAI**：响应式断点 1120/860 → **700**（iPad 竖屏/横屏回桌面侧栏布局，仅手机 ≤700 顶部菜单；顶部菜单 6 列改 3 列）；AppShell 初始 loadState='ready'（后台刷 health，不等待 /api/health 往返）；**路由全部同步加载**（原 React.lazy 首访白屏，页面均小故直接同步）。
5. **演示提示条（polish-previews.mjs）**：`HOST_CANDIDATES` 优先 `.workspace-main/.app-content/.page-content/.ant-layout-content`，findHost 跳过 `app-shell|side-rail|shell|sidebar|nav|sider|menu`；顶部横条 `#demo-mode-note` 统一 **absolute 浮层**（宿主补 relative，不占流，防止把固定视口内容挤出去）。重跑：`node scripts/polish-previews.mjs`。
6. 图标体系：主站 + 5 个预览页 + 404.html 均有 favicon + 180×180 apple-touch-icon；`scripts/gen-preview-icons.ps1` 生成（SyLabAI 用韶远/Accela 徽标裁剪，模板图在 `scripts/assets/shoayuan-emblem.png`）。

### 站点内容
- **About 双卡**：左侧"三段经历 · 从数据到交付"（早期/近期/日常通用化叙述，无分点、无教育背景重复）；右侧"多岗位定位 · 各方向均可验证"（AI 应用与工具链 / 企业系统与后端交付 / 数据工程与推荐 / IT 支持与运维，每方向 3-4 个代表性关键词标签，绿色加粗子标题，无项目圆点；数据在 profile.json `positions`，类型 `AboutPosition`）。
- **技能区**：7 个画像（通用 / AI 全栈开发 / **数据工程与大数据** / ERP 实施 / AI 数据质检 / 助教·培训 / 其他·过渡兼职）；数据方向独立（数据采集与清洗 / 数据库（含 SQL Server、备份恢复）/ 大数据与推荐）。
- **项目区**：MiLuAssistantDesktop 下载改为 **GitHub Release 资产直链**（`…/releases/download/v1.0.1/MiLuAssistantDesktop-Setup-1.0.1.exe`）+ 可见提示"约 213MB · 点击直接下载 · GitHub 受限可稍后重试"（`downloadHint` 字段，DB 有 `download_hint` 列；手机端提示独占一行、动作链接 whitespace-nowrap）。

## 三、待办 / 未开始

1. **站点首屏字体 FOUT**：根因 = 全部 `@font-face` 用 `font-display: swap` 且无预加载（字体在 CSS 解析后才请求）→ 首帧后备字体、到达后"覆盖式"切换（蒋宇龙名字/得意黑小标题最明显）。
   **拟方案**（待确认，未实施）：在 `main.tsx` 顶部用 Vite 资源导入预加载 4 个首屏字体（liu-jian-mao-cao 10KB / smiley-sans 116KB / noto-sans-sc-400 121KB / noto-sans-sc-900 123KB）：
   ```ts
   import LiuJianUrl from './fonts/liu-jian-mao-cao-regular.woff2'
   // 对4个都执行: link rel=preload as=font type=font/woff2 crossorigin=anonymous appendChild(head)
   ```
   保持 `font-display: swap`（避免 FOIT 白字）。构建后 URL 由 Vite 解析，无需改 index.html。
2. **BookRecommendation 首访慢**（见下）。
3. MiLuStudio 手机端侧栏显示优化（用户后续单独做）。
4. （可选）MiLuAssistantWeb 主包 6.35MB 路由级分包；XiaoLouAI 类似。

## 四、BookRecommendation 专项

- 位置：`D:\code\BookRecommendation`（Vue CLI，前端在 `frontend/`，`vue.config.js` publicPath 已支持 embedded 相对路径；预览产物在 jyl-site `public/preview/book-recommendation/`）。
- **已完成优化（2026-09-06 实施）**：
  1. ✅ 路由懒加载（`router/index.js` 全部 `component: () => import(...)`，按 auth/book/user 分包）；
  2. ✅ Element UI 按需引入（`babel-plugin-component`（已加入 devDependencies）+ `plugins/element.js` 按需注册全部用到的组件 + 移除 main.js 全量 `Vue.use(ElementUI)`；`ElementUI.Message` → `Vue.prototype.$message`）；
  3. ✅ ECharts 按需引入（`Home.vue`：`echarts/core` + LineChart + Grid/Tooltip/Title component + CanvasRenderer；**注意：不能用动态 import()，webpack 会把全部图表打成一个 4.8MB 的 echarts chunk，静态按需才是 2.2MB**）。
  - 效果：chunk-vendors.js 8.09MB → **2.34MB**（gzip 368KB）；Home chunk 2.17MB（gzip 391KB）；首屏 gzip 总 ~768KB。
  - 构建：`cd frontend && npm run build -- --mode embedded`（需先 `npm install babel-plugin-component`）；将 `frontend/dist` 全目录拷贝到 `jyl-site/public/preview/book-recommendation/`（保留 apple-touch-icon.png），再 `node scripts/polish-previews.mjs`。
- **当前站点状态**：预览正常；演示自动登录 + 数据 mock 已内置（`src/mock/mocks`）。

## 四·五、MiLuAssistantWeb 预览专项（2026-09-06）

- **字体完全本地化已完成**：console 移除 index.html 的 Google Fonts（Inter/Lato/Newsreader）链接；改用 `@fontsource/inter|@fontsource/lato|@fontsource/newsreader`（devDependencies），入口 `src/styles/fonts-local.css`（latin 子集 400/500/600、Lato 400/700、Newsreader 400/500/400i，共 8 个 woff2 ~190KB）；`src/main.tsx` 引入。构建产物不含任何 fonts.googleapis/gstatic 请求。
- 构建：`console: npm run build -- --base=./` → 拷贝 assets（**注意用 `Copy-Item $src\assets\* $dst\assets -Recurse`，直接拷目录会嵌套 assets/assets**）+ index.html + logo.png/dark-logo.png/milu-local-288.png。

## 五、预览重建与同步速查

| 预览 | 构建命令（源仓库） | 复制目标 |
|---|---|---|
| milu-studio | `apps/web`: `npm run build`（类型）→ `npx vite build --mode embedded --base=./` | `jyl-site/public/preview/milu-studio/`（assets+index.html+brand） |
| milu-assistant-web | `console`: `npm run build -- --base=./` | `public/preview/milu-assistant-web/`（assets+index.html+logo.png/dark-logo.png/milu-local-288.png） |
| xiao-lou-ai | `XIAOLOU-main`: `npx vite build --base=./` | `public/preview/xiao-lou-ai/`（assets+index.html+chuangjing-favicon-32.png+chuangjing-logo-shell.png） |
| sylab-ai | `SyLabAI/apps/web`: `npx vite build --base=./` | `public/preview/sylab-ai/`（assets+index.html） |
| book-recommendation | `BookRecommendation/frontend`: `npm run build -- --mode embedded` | `public/preview/book-recommendation/`（index.html+js/img/fonts 全目录） |

- **polish-previews.mjs 横条逻辑（2026-09-06 更新）**：`#demo-mode-note` 统一 absolute 浮层；MiLuConsole 特例 `CHAT_HEADER_OFFSET=true`——检测 `[class*="chat-anywhere-header"]`（64px，含新建会话/模型选择按钮）自动把横条下移到 header 下方（`top = header高度+12`），避免遮挡交互；`positionNote(n, host)` 独立函数，header 出现/路由切换后 `setInterval` 自动重定位。
- 每次重建后：**检查各预览 index.html 的 apple-touch-icon 链接**（Vite 产物不含，需补 `<link rel="apple-touch-icon" sizes="180x180" href="./apple-touch-icon.png?v=1" />`）；然后 `node scripts/polish-previews.mjs`；`npm run db:seed`（若数据变更）→ `npm run build` → 提交推送。
- `scripts/gen-preview-icons.ps1`（pwsh 运行，变量名避免与参数同名——PowerShell 变量大小写不敏感）可重新生成各预览 apple-touch-icon。
